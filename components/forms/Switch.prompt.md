On/off toggle (role="switch") for settings and preferences.

```jsx
<Switch label="Eco-friendly products" defaultChecked />
<Switch label="Text me when the team is on the way" />
```

- Forwards `checked`, `defaultChecked`, `onChange`, `disabled`.
