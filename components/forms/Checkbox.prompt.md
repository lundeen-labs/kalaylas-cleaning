Labelled checkbox on a native input. Use for opt-ins and multi-select lists.

```jsx
<Checkbox label="Send me appointment reminders" defaultChecked />
<Checkbox label="I agree to the terms" />
```

- Forwards `checked`, `defaultChecked`, `onChange`, `disabled`.
